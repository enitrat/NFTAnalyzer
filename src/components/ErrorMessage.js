function ErrorMessage() {
    return (
      <div className="centeredTitle">
        <h1 className="welcomeTitle">There was an error while fetching the data</h1>
      </div>
    );
  }
  
  export default ErrorMessage;
  